searchState.loadedDescShard("assert_float_eq", 0, "Assertions that two floating point numbers are …\nAssert two 32-bit floats are not within <code>n</code> steps of each …\nAssert two 32-bit floats are within <code>n</code> steps of each other.\nAssert two 64-bit floats are not within <code>n</code> steps of each …\nAssert two 64-bit floats are within <code>n</code> steps of each other.\nAssert the absolute error between two values is less than …\nAssert the absolute error between two values is greater …\nAssert the relative error between two values is less than …\nAssert the relative error between two values is greater …\nExpect two 32-bit floats are not within <code>n</code> steps of each …\nExpect two 32-bit floats are within <code>n</code> steps of each other.\nExpect two 64-bit floats are not within <code>n</code> steps of each …\nExpect two 64-bit floats are within <code>n</code> steps of each other.\nExpect the absolute error between two values is less than …\nExpect the absolute error between two values is greater …\nExpect the relative error between two values is less than …\nExpect the relative error between two values is greater …")